# Variables de entorno para React + Vite
# Copia este archivo como .env.local y ajusta la URL de la API PHP

# URL base de la API PHP (sin barra al final)
# Desarrollo con XAMPP: la carpeta /api/ debe estar en htdocs/api
VITE_API_URL=http://localhost/api
